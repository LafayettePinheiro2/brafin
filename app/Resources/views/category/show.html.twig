{% extends 'base.html.twig' %}

{% block body %}
    
    <div class="container">
        <div class="row">
            <div class="col-md-12">
                <h1 class="pull-left">Category view</h1>
                <a href="{{ path('category_index') }}" class="btn btn-primary pull-right" role="button">Back to category list</a>
                <a href="{{ path('category_edit', { 'id': category.id }) }}" class="btn btn-primary pull-right" role="button">Edit</a>
                
                {{ form_start(delete_form) }}
                    <input class="btn btn-primary pull-right" type="submit" value="Delete">
                {{ form_end(delete_form) }}                
            </div>
        </div>
        <div class="row">
            <!-- category ITEM -->
            <div class="col-md-3">
                <div class="panel price panel-grey">
                    <div class="panel-heading arrow_box text-center">
                        <h3>{{ category.name }}</h3>
                    </div>
                    <ul class="list-group list-group-flush text-center">
                        <li class="list-group-item"><i class="icon-ok text-success"></i> {{ category.name }}</li>
                        <li class="list-group-item"><i class="icon-ok text-success"></i> <a href="{{ path('category_edit', { 'id': category.id }) }}">edit</a></li>
                    </ul>
                </div>
            </div>
            <!-- /category ITEM -->
        
        </div>
    </div>
{% endblock %}
